import { ComingSoon } from "@/components/coming-soon";

export default function TransfersPage() {
  return (
    <ComingSoon
      eyebrow="Decision room"
      title="Transfer studio"
      description="Stage one move, then ask the existing optimiser how many transfers survive the hit."
    />
  );
}
