# Core pipeline (scripts/ and final.ipynb)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
joblib>=1.3
matplotlib>=3.7
seaborn>=0.12

# Squad optimisation (final.ipynb: build_best_predicted_team, suggest_transfers)
pulp>=2.7

# Live FPL API pull (using_api.ipynb, advanced_fpl_models.ipynb)
requests>=2.31

# Web UI (webapp/app.py). Wraps scripts/optimise.py; no extra modelling deps.
flask>=3.0

# FBref defensive-stat scraping (fetching_defensive_data.ipynb).
# Only needed if you re-fetch defensive data; build_dataset.py carries the
# existing columns over instead.
soccerdata>=1.6
rapidfuzz>=3.0
